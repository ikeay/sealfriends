<h1>Listing introductions</h1>
<table>
  <tr>
    <th>Name</th>
    <th>Link</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= link_to "Page Link", "#{user.id}" %></td>
  </tr>
<% end %>
</table>

<br />
